import { createTheme } from '@mui/material/styles'

const theme = createTheme({
	palette: {
		primary: {
			main: '#303030',
		},
		secondary: {
			main: '#f50057',
		},
		background: {
			default: '#1d1515',
		},
	},
})

export default theme
